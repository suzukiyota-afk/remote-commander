# Remote Commander — frontend (GitHub Pages)

This folder is published to GitHub Pages. On first load the app asks for the
Cloudflare Tunnel URL of the Python backend running on your PC. The URL is
saved in `localStorage`; paste the new one whenever the tunnel restarts.
